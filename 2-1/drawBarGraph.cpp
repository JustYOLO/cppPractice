// unfinished
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

void drawBarGraph(vector<int> v)
{
    int big = *max_element(v.begin(), v.end());
    for(int i = big; i >= 0; i--)
    {

    }
}

int main()
{
    vector<int> v;
    v.push_back(10);
    v.push_back(12);
    v.push_back(4);



    return 0;
}